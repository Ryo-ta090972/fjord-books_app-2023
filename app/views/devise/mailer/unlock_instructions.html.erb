<p><%= t('views.common.hello', name: @resource.email) %>!</p>

<p><%= t('devise.failure.locked') %></p>

<p><%= t('views.common.link_below.unlock_account') %></p>

<p><%= link_to t('views.common.unlock_account'), unlock_url(@resource, unlock_token: @token) %></p>
